[analysis.config.StaticAnalysisConfig]
; This codebase keeps comments minimal by policy, so do not require doc comments.
undocumented_declaration_check="disabled"
; Style-only and noisy on test locals.
could_be_immutable_check="disabled"
